"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const WFParameter_1 = require("./WFParameter");
class WFVariablePickerParameter extends WFParameter_1.WFParameter {
    constructor(data, name = "Variable Picker", docs = "https://pfgithub.github.io/shortcutslang/gettingstarted#variable-picker-fields") {
        super(data, name, docs);
    }
    genDocsArgName() {
        return `(v:myvar | mv:myvar | s:myvar)`;
    }
    genDocs() {
        return `${super.genDocs()}

Accepts a variable.`;
    }
    build(cc, parse) {
        if (!parse.canBeVariable(cc)) {
            throw parse.error(cc, "Variable picker fields only accept variables.");
        }
        const variable = parse.asVariable(cc);
        return variable;
    }
}
exports.WFVariablePickerParameter = WFVariablePickerParameter;
