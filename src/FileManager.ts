export class FileManager {
	constructor() {}
	listItems() {
		// returns a list of files and folders
	}
	createFolder() {}
	saveFile(path: string, data: string) {
		// saves the file at the path
	}
	loadFile(path: string, data: string) {}
	renameFile(path: string, newPath: string) {}
}
