"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const WFParameter_1 = require("./WFParameter");
class WFContentArrayParameter extends WFParameter_1.WFParameter {
    constructor(data, name, docs = "https://pfgithub.github.io/shortcutslang/gettingstarted#list-field") {
        super(data, name, docs);
    }
    genDocsArgName() {
        return `[list, items]`;
    }
    genDocs() {
        return `${super.genDocs()}

Accepts a list.`;
    }
    build(cc, parse) {
        if (!parse.canBeList(cc)) {
            throw parse.error(cc, "List fields only accept lists.");
        }
        const list = parse.asList(cc);
        return list;
    }
}
exports.WFContentArrayParameter = WFContentArrayParameter;
