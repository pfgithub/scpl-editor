"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const WFParameter_1 = require("./WFParameter");
class WFDictionaryParameter extends WFParameter_1.WFParameter {
    constructor(data, name = "Dictionary", docs = "https://pfgithub.github.io/shortcutslang/gettingstarted#dictionary-field") {
        super(data, name, docs);
    }
    genDocsArgName() {
        return `{dictionary}`;
    }
    genDocs() {
        return `${super.genDocs()}

Accepts a dictionary.`;
    }
    build(cc, parse) {
        if (!parse.canBeDictionary(cc)) {
            throw parse.error(cc, "Dictionary fields only accept dictionaries.");
        }
        return parse.asDictionary(cc);
    }
}
exports.WFDictionaryParameter = WFDictionaryParameter;
